# API Configuration
# For local development, use: http://localhost:3001
# For production (Vercel), leave this empty or remove it to use relative paths
VITE_API_URL=http://localhost:3001

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password-here

# Set to 'true' to use mock email (no real emails sent) for testing
MOCK_EMAIL=false
