name: Daily AI Code Updater (MiniMax)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build-and-commit:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install @anthropic-ai/sdk

      - name: Run AI Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          ANTHROPIC_BASE_URL: "https://api.minimax.io/anthropic"
        run: node auto-agent.js

      - name: Commit and Push changes
        run: |
          git config --local user.email "aphisityns170960@gmail.com"
          git config --local user.name "aphsx"

          if [ -n "$(git status --porcelain)" ]; then
            git add .
            git commit -m "chore: automated daily update by MiniMax AI [skip ci]"
            git push
          else
            echo "No changes detected. Skipping commit."
          fi